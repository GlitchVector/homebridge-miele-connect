{
  "pluginAlias": "MieleConnect",
  "pluginType": "platform",
  "singular": true,
  "customUi": true,
  "headerDisplay": "Bridges Miele@home appliances into HomeKit via the Miele 3rd-party cloud API (REST + Server-Sent Events). Use the **Miele OAuth pairing** card above to obtain credentials at https://developer.miele.com/ and complete the OAuth code-exchange — tokens are persisted server-side under Homebridge's data directory.",
  "footerDisplay": "",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Platform Name",
        "type": "string",
        "default": "Miele Connect",
        "required": true
      },
      "clientID": {
        "type": "string"
      },
      "clientSecret": {
        "type": "string"
      },
      "accessToken": {
        "type": "string"
      },
      "refreshToken": {
        "type": "string"
      },
      "language": {
        "title": "API Language",
        "type": "string",
        "description": "Optional. Two-letter language code (e.g. 'de', 'en') for localised Miele enum strings."
      },
      "reconnectEventStreamMinutes": {
        "title": "Periodic SSE Reconnect (minutes)",
        "type": "integer",
        "default": 60,
        "minimum": 5,
        "description": "How often to cycle the SSE event stream proactively. Defends against load-balancer idle drops; 60 minutes is fine for most setups."
      }
    }
  },
  "form": [
    "name",
    "language",
    "reconnectEventStreamMinutes"
  ]
}
